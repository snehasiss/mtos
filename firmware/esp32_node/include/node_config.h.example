#pragma once
#define MTOS_NODE_ID "N001"
#define MTOS_FIRMWARE_VERSION "0.1.0"
#define MTOS_CONFIGURATION_REVISION 1
#define MTOS_SHIFT_DATA_PIN 23
#define MTOS_SHIFT_CLOCK_PIN 18
#define MTOS_SHIFT_LATCH_PIN 5
#define MTOS_SHIFT_OE_PIN 19
#define MTOS_MACHINE_RELAY_PIN 27
#define MTOS_SHIFT_REGISTER_COUNT 2

struct ServoComponent { uint8_t channel; uint16_t straight; uint16_t diverging; };
struct TurnoutMap { const char *id; uint8_t count; ServoComponent servo[2]; };
struct SignalMap { const char *id; uint32_t stopMask; uint32_t slowMask; uint32_t goMask; bool threeAspect; };

static const TurnoutMap TURNOUTS[] = {
  {"T001", 1, {{0, 210, 390}, {0, 0, 0}}},
};
static const SignalMap SIGNALS[] = {
  {"G001", 1UL << 0, 0, 1UL << 1, false},
};
static const uint32_t BUFFER_MASK = 1UL << 15;
static const char *WATER_TANK_ID = "E001";
static const uint32_t WATER_TANK_CONTACT_MS = 200;       // commission before use
static const uint32_t WATER_TANK_CYCLE_MS = 30000;       // commission before use
