# MTOS runtime dependencies; keep aligned with pyproject.toml.
# Requires Python >= 3.11. A virtual environment is optional, not required.
# Flask installs its supporting dependencies, including Werkzeug and Jinja2.
# sqlite3 and json are Python standard-library modules, not pip packages.
Pillow>=10
Flask>=3.1,<4
waitress>=3,<4
pyserial>=3.5,<4
Flask-SocketIO>=5.5,<6
simple-websocket>=1.1,<2
paho-mqtt>=2.1,<3
